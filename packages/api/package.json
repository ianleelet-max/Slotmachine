{
  "name": "@auditreq/api",
  "version": "0.1.0",
  "description": "API AudiTREQ — recherche, graphe, UBO, détection",
  "type": "module",
  "main": "./dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "npm run build && node dist/index.js",
    "start": "node dist/index.js",
    "seed": "npm run build && node dist/seed.js",
    "db:init": "npm run build && node dist/init-db.js"
  },
  "dependencies": {
    "@fastify/cors": "^10.0.1",
    "@auditreq/core": "*",
    "fastify": "^5.2.0",
    "pg": "^8.13.1",
    "@auditreq/capture": "*"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "@types/pg": "^8.11.10",
    "typescript": "^5.7.2"
  }
}
