{
  "name": "@velocereq/ingestion",
  "version": "0.1.0",
  "description": "Ingestion des données ouvertes du Registraire des entreprises du Québec",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test \"dist/**/*.test.js\"",
    "ingerer": "npm run build && node dist/cli.js"
  },
  "dependencies": {
    "@velocereq/core": "*"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2"
  }
}
